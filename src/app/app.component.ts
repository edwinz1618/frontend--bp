import { Component } from '@angular/core';

@Component({
  selector: 'app-root',
  template: `<app-container-principal/>
            <app-modal></app-modal>`,
})
export class AppComponent {
  title = 'front-end-bp';
}
